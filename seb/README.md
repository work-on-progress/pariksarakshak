# Put your built `.seb` file here

This folder holds the Safe Exam Browser configuration you build by following
`docs/06-SEB-CONFIGURATION.md`. Save it as `pariksarakshak.seb`.

The file stores its passwords encrypted, but treat it as sensitive anyway:
distribute it from your department share drive rather than a public repository,
and never publish the quit password alongside it.
